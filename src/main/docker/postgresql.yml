version: '2'
services:
  zartacms-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/ZartaCMS/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ZartaCMS
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
